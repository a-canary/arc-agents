# rpn — reverse-Polish calculator
`rpn EXPR` evaluates a reverse-Polish expression passed as a single argument.
Space-separated tokens are integers and the operators + - * /. Division is the
integer division of evenly-divisible operands. Prints the integer result.
Example: `rpn "3 4 +"` prints `7`.
