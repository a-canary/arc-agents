# wc-lite — minimal word count
Reads stdin. Takes exactly one flag and prints a single number:
- `-l` : number of newline characters in the input
- `-w` : number of whitespace-separated words
- `-c` : total number of bytes
Output is just the number (no filename, no padding).
