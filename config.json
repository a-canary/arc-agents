{
  "_note": "Proxy-routed factory lane (arc-llm-proxy, http://127.0.0.1:8091). Consumers only know arc-proxy/<alias> — never Veles, llama-103, or minimax endpoints directly. Workhorse + lite + driver + bench route through the proxy so the proxy enforces slots, audit logging, and rate/balance tracking. Escalation (defense, really difficult): claude-afk opus-medium as primary admin carve-out, with arc-proxy/minimax-m3 failover (Qwen3.8 direct is retired). opus-max is forbidden — never use it. Hard aliases (hard, and any defense/escalation alias) follow the same rule: opus-medium primary, arc-proxy/minimax-m3 failover.",
  "exec_cli_alias": {
    "planning": [
      "pi --model arc-proxy/planning -p {prompt}"
    ],
    "driver": [
      "pi --model arc-proxy/driver -p {prompt}"
    ],
    "bench": [
      "pi --model arc-proxy/bench -p {prompt}"
    ],
    "easy": [
      "pi --model arc-proxy/easy -p {prompt}"
    ],
    "hygiene": [
      "pi --model arc-proxy/hygiene -p {prompt}"
    ],
    "hard": [
      "claude-afk --model opus --effort medium -p {prompt}",
      "pi --model arc-proxy/minimax-m3 -p {prompt}"
    ]
  },
  "pool_caps": { "interactive": 6, "ops": 5, "build": 4, "explore": 4, "default": 4 },
  "default_alias": "planning",
  "fast_alias": "easy",
  "smart_alias": "planning"
}
