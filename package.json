{
  "name": "arc-agents",
  "version": "0.1.0",
  // pinned 0.x for explicit SemVer contract — every bump is a deliberate act
  // (no ^, no ~, no range). See docs/PUBLIC-API.md §Version pinning policy.
  "type": "module",
  "private": true,
  "bin": {
    "ledger": "./bin/ledger.ts",
    "wait-for-ledger": "./bin/wait-for-ledger.ts",
    "arc-chat": "./bin/arc-chat.ts",
    "arc-ux": "./bin/arc-ux.ts",
    "arc-tui": "./bin/arc-tui.ts",
    "arc-replay": "./bin/arc-replay.ts",
    "factory": "./bin/factory.ts",
    "webui-server": "./bin/webui-server.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^3.23.0"
  }
}
