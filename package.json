{
  "name": "arc-agents",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "ledger": "./bin/ledger.ts",
    "arc-launch": "./bin/launch.ts",
    "wait-for-ledger": "./bin/wait-for-ledger.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^3.23.0"
  }
}
