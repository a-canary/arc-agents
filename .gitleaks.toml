title = "arc-agents gitleaks config"
# Allowlist: Discord user snowflake matching discord-client-id rule is NOT a client secret.
# Suppressed by path-based allowlist targeting the scan-report file itself.

[allowlist]
  description = "Known false positives"
  # .gitleaksignore is a scan-report artifact; its findings are not actual leaks.
  paths = [".gitleaksignore"]
